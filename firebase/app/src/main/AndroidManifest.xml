<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.greatlove.musicplay">

    <application
        android:name=".activity.Helpservices"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.AdminActivity"
            android:theme="@style/AppTheme1">
        </activity>
        <activity
            android:name=".activity.FolderSongsActivity"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".activity.ShowAllSongsActivity"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".activity.FolderSelectActivity"
            android:theme="@style/AppTheme1" />
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".activity.ShowSongsActivity"
            android:theme="@style/AppTheme1" />
        <activity android:name=".activity.SignupActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.ProviderSelection">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>